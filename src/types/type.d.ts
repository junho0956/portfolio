type RECORD = {
    timestamp: string;
    title: string;
    subTitle: string;
    records: string[];
}

type PROJECT = {
    type?: string;
    timestamp: string;
    title: string;
    serviceUrl: string;
    description: string[];
    detailUrl: string;
}
